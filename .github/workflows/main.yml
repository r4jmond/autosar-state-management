name: 'AUTOSAR CI'

on:
  workflow_dispatch:
  push:
    branches:
      - '**' 
  pull_request:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: '${{ github.workflow }} @ ${{ github.event.pull_request.head.label || github.head_ref || github.ref }}'
  cancel-in-progress: true

jobs:
  Build_Run_UTs:
    runs-on: ${{ fromJSON('["ubuntu-latest", "self-hosted"]')[github.repository == 'github/docs-internal'] }}
    steps:
      - name: Install lcov
        run: sudo apt-get install -y lcov

      - name: Install gcovr
        run: pip install gcovr
      - name: Checkout
        uses: actions/checkout@v3
        with:
            submodules: recursive

      - name: Make build directory
        run: mkdir build

      - name: Create cmake configuration
        working-directory: build
        run: cmake ..

      # - name: Build
      #   working-directory: build
      #   run: make all

      - name: Build UTs
        working-directory: build
        run: make gTests_all

      - name: Run UTs
        run: ./build/tests/gTests_all --gtest_output=xml:./test-reports/UTs_report.xml

      - name: XUnit Viewer
        id: xunit-viewer
        uses: AutoModality/action-xunit-viewer@v1  
        if: always()

      - name: Attach the GTest Report
        id: gTestReport
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: Google-Test-Report
          path: ${{ steps.xunit-viewer.outputs.report-dir }}/index.html

      # - name: HTML Preview UTs
      #   id: html_preview_ut
      #   uses: pavi2410/html-preview-action@v2
      #   if: always()
      #   with:
      #     html_file: ${{ steps.gTestReport.outputs. }}/index.html

      # - name: Print_link_to_html_UT
      #   run: echo ${{steps.html_preview_ut.outputs.url}} >> $GITHUB_STEP_SUMMARY

      - name: Build coverage
        working-directory: build
        run: make gTests_coverage

      - name: Attach the Coverage Report
        id: gcovReport
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: Coverage-Report
          path: build/gTests_coverage/index.html

      # - name: HTML Preview Coverage
      #   id: html_preview_cov
      #   uses: pavi2410/html-preview-action@v2
      #   if: always()
      #   with:
      #     html_file: build/gTests_coverage/index.html

      # - name: Print_link_to_html_cov
      #   run: echo ${{steps.html_preview_cov.outputs.url}} >> $GITHUB_STEP_SUMMARY

  Linters:
    runs-on: ${{ fromJSON('["ubuntu-latest", "self-hosted"]')[github.repository == 'github/docs-internal'] }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run cppcheck
        uses: deep5050/cppcheck-action@v3.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN}}
          other_options: --language=c++ --std=c++14 --enable=all --addon=cert.py

      - name: Read Cppcheck results
        id: cppcheck
        uses: juliangruber/read-file-action@v1
        with:
          path: ./cppcheck_report.txt

      - name: Echo Cppcheck results
        run: echo "${{ steps.cppcheck.outputs.content }} >> $GITHUB_STEP_SUMMARY"

      # - name: Archive cppcheck results
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: cppcheck-report
      #     path: cppcheck_report.txt

  # Run UTs:
  #   needs: Build
  #   runs-on: ${{ fromJSON('["ubuntu-latest", "self-hosted"]')[github.repository == 'github/docs-internal'] }}
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v3
  #       with:
  #         submodules: recursive

  #     - name: Make build directory
  #       run: mkdir build

  #     - name: Create cmake configuration
  #       working-directory: build
        # run: cmake .. -DCMAKE_BUILD_TYPE=Debug ..


#      - name: Archive coverage results
#        uses: actions/upload-artifact@v3
#        with:
#          name: code-coverage-report
#          path: build/coverage_report/

      # - name: Code Coverage Summary Report
      #   uses: irongut/CodeCoverageSummary@v1.3.0
      #   with:
      #     filename: build/coverage_report.xml
