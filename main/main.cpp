#include "Employee.h"

/* This file is only for internal development purposes, our general code should be in the src directory
 * and tested with tests in tests directory. */

int main()
{
    Employee employee = Employee(25, "Foobar");
    return 0;
}
